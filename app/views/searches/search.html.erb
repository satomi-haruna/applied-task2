<!--模範解答より、このビューはuser/indexやbook/indexを使用できる-->
<!--＜% if @range == "User" %＞
      <h3>Users search for "＜%= @word%＞"</h3>
      ＜%= render 'users/index', users: @users＞
      ↑user/indexのusersをsearchの@usersに置き換える、searchの@usersは別の変数名の方がいいかな
    ＜% elsif @range == "Book" %＞
      <h3>Books search for "＜%= @word %＞"</h3>
      ＜%= render 'books/index', books: @books %＞
      usersと同様に、book/indexのbooksをsearch結果の変数に置き換えて、一覧画面を使用する！コードがこれだけで済む-->

<h2><%= @range %>s search for "<%= @word %>"</h2>

<table class="table">
<% if @range == "User" %>
  <thead>
    <tr>
      <th></th>
      <th>Name</th>
      <th>Introduction</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
    <tr>
      <td><%= image_tag user.get_profile_image, size: '50x50' %></td>
      <td><%= link_to user.name, user_path(user.id) %></td>
      <td><%= user.introduction %></td>
    </tr>
    <% end %>
  </tbody>
<% else %>
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
    <tr>
      <td><%= image_tag book.user.get_profile_image, size: '50x50' %></td>
      <td><%= link_to book.title, book %></td>
      <td><%= book.body %></td>
    </tr>
    <% end %>
  </tbody>
<% end %>
</table>
